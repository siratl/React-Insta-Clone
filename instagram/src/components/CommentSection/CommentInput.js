import React from 'react';
const CommentInput = props => {
  return (
    <form>
      <input className="commentInput" placeholder="Add comment" type="text" />
    </form>
  );
};

export default CommentInput;
